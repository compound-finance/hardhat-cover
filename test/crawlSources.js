const assert = require('assert');
const lib = require('../dist/index.js');

describe('crawlSources', () => {
  it.skip('xxx crawls sources', async () => {
    // XXX copy artifacts from comet? how to load? make this a hardhat dir?
  });
});